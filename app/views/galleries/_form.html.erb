<% form_for @gallery, :html => { :multipart => true } do |f| %>
  <%= f.error_messages %>
  <p>
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.label :user_id %><br />
    <%= f.text_field :user_id %>
  </p>
  <p>
    <%= f.label :shoot_date %><br />
    <%= f.date_select :shoot_date %>
  </p>

  <p>
    <%= f.label :collection %><br />
    <% if @gallery.collection.file? %>

      <%= link_to "Add photos to gallery from: ",
        :action => 'extract', :id => @gallery.id %>
      <%= @gallery.collection.original_filename %><br />
    <% end %>

    <%= f.file_field :collection %>
  </p>
  <p><%= f.submit %></p>
<% end %>
